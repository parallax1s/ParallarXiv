# ParallarXiv

This repo hosts a simple demo site. Paper data is generated at deploy time using `generate_papers.py`.

## Generating data

Run `python generate_papers.py` to create `papers.json` with 5,000 artificial papers across multiple science topics.
